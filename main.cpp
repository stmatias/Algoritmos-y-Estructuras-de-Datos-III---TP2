
#include <vector>
#include <iostream>
#include <utility>
#include <limits>
#include <stack>
using namespace std;


int main() {
	/*
	//Asumiremos que -1 es indefinido o ya agregado
	vector<vector<int> > M = { {-1, 1,2 ,100},
								{1, -1, 1,2},
								{2, 1, -1,1},
								{100, 2, 1,-1},
	};
	vector<vector<int>> res = vecinoMasCercano(M);
	vector<vector<int>> res = vecinoMasCercano(M);
	imprimirSolucion(res[1]);
	*/
	//cout << "El resultado es: " << " n: " << res[0][0] << " Costo: " << res[0][1] << " La solucion es: " << endl;
	cout << "Funciona" << endl;
	return 0;
}
